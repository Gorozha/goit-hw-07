// const elements = document.querySelectorAll(".item");
// elements.forEach(function (element) {
//   console.log(`Категория: ${element.firstElementChild.textContent}`);
//   console.log(
//     `Количество элементов: ${element.lastElementChild.children.length}`
//   );
// });

// "use strict";

// const totalCategories = document.querySelectorAll(".item");
// console.log(`В списке ${totalCategories.length} категории.`);

// const categoriesArray = [...totalCategories]
//   .map(
//     (categories) => `Категория: ${categories.children[0].textContent}
// Количество элементов: ${categories.children[1].children.length}`
//   )
//   .join("\n");
// console.log(categoriesArray);
//===============

//================== HW-7-1

// const elements = document.querySelectorAll(".item");
// console.log(`В списке ${elements.length} категории.`);
// elements.forEach(function (element) {
//   console.log(`Категория: ${element.firstElementChild.textContent}`);
//   console.log(
//     `Колитчество элементов: ${element.lastElementChild.children.length}`
//   );
// });
//======================================================================
//console.log(elements);

// const elements1 = document.querySelectorAll(".item li");
// console.log(elements1);

// const elements2 = document.querySelectorAll(".item ul");
// console.log(elements2);
